global:
   arguments: 
     aws_default_region: eu-west-1
labels:
   general: 
     environment: dev
     purpose: eval-ed
   infrastructure:
     cloud: aws
     product: ec2
     area: server
selectors:
   network_vars:
     labels:
       environment: dev
       purpose: eval-ed
       area: network
       region: eu-west-1
       cloud: aws
infrastructure:
   ec2:
       stack_name: elasticdev:::aws_ec2_server
       arguments:
          key_name: default
          sg_id: selector:::network_vars::web_sg_id
          subnet_ids: selector:::network_vars::public_subnet_ids
          hostname: test-demo
          remote_stateful_bucket: s3://app-env.customer-provider.tfstatefile
          cloud_tags_hash:
            environment: dev
            purpose: eval-ed
       to_base64:
         - cloud_tags_hash
       labels:
         - general
         - infrastructure
       selectors:
         - network_vars
       credentials:
         - reference: eval-ed-iam
           orchestration: true

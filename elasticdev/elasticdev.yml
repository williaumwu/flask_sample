labels:
   general: 
     environment: dev
     purpose: test
   infrastructure:
     product: webhook
infrastructure:
   webhook:
       stack_name: elasticdev:::github_webhook
       arguments:
          name: ed-private-test-webhook
          repo: ed-private-test
          url: https://app-api.elasticedev.io/webhook
       labels:
          - general
          - infrastructure
       inputvars:
           - reference: github-token
             orchestration: true


global_arguments:
   aws_default_region: us-east-2
   vpc_name: tier_vpc
infrastructure:
   parse-terraform:
       stack_name: elasticdev:::parse_terraform
       arguments:
          src_resource_type: vpc
          resource_type: subnet
          mode: managed
          resource_name: tier_vpc
          vpc: tier_vpc
          must_exists: true
          provider: aws

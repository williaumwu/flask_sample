labels:
   general: 
     environment: dev
     purpose: test
infrastructure:
   codebuild:
       stack_name: elasticdev:::codebuild_ci
       arguments:
          codebuild_name: flask-sample-private
          bucket_acl: private
          bucket_expire_days: 2
          aws_default_region: eu-west-1
       credentials:
         - reference: eval-ed-iam
           orchestration: true
       labels:
          - general

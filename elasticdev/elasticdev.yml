labels:
   general: 
     environment: dev
     purpose: ci
infrastructure:
   lambda:
       stack_name: elasticdev:::aws_dynamodb
       arguments:
          aws_default_region: eu-west-1
          dynamodb_name: codebuild-ci
          cloud_tags_hash:
            billing_reference: database
            environmental: dev
            phase: infrastructure
       to_base64:
         - cloud_tags_hash
       credentials:
         - reference: eval-ed-iam
           orchestration: true
       labels:
          - general

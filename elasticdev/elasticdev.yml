labels:
   general: 
     environment: dev
     purpose: ci
global_arguments:
   aws_default_region: eu-west-1
   ci_environment: ed-eval
   suffix_id: d645633
   cloud_tags_hash:
       environment: dev
       purpose: ci
       billing_code: 87deo
infrastructure:
   setup_codebuild:
       stack_name: elasticdev:::setup_gitlab_ci
       arguments:
          visibility_level: private
       to_base64:
         - cloud_tags_hash
       credentials:
         - reference: eval-ed-iam
           orchestration: true
       inputvars:
           - reference: gitlab-parent-group
             orchestration: true
           - reference: gitlab-token
             orchestration: true
       labels:
           - general

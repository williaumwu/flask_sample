global_arguments:
   aws_default_region: ap-southeast-1
   vpc_name: mongodb-vpc
   mongodb_cluster: mongodb-dev
infrastructure:
   ssh_upload:
       stack_name: elasticdev:::ec2_ssh_upload
       arguments:
          name: mongodb-ssh-dev
       credentials:
           - reference: aws_2
             orchestration: true

global_arguments:
   aws_default_region: ap-southeast-1
   vpc_name: sample
infrastructure:
   ssh_upload:
       stack_name: elasticdev:::ec2_ssh_upload
       arguments:
          name: mongodb_ssh
       credentials:
           - reference: aws_2
             orchestration: true

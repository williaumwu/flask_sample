labels:
   general: 
     environment: dev
     purpose: test
   infrastructure:
     cloud: do
infrastructure:
   ssh_upload:
       stack_name: elasticdev:::do_ssh_upload
       arguments:
          name: ed-test-do-key
       labels:
         - general
         - infrastructure
       credentials:
           - reference: do
             orchestration: true

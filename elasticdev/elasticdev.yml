labels:
   general: 
     environment: dev
     purpose: test
   infrastructure:
     cloud: do
infrastructure:
   ssh_upload:
       stack_name: elasticdev:::new_do_ssh_key
       arguments:
          name: ed-test-do-key
       labels:
         - general
         - infrastructure
       credentials:
           - reference: do
             orchestration: true
